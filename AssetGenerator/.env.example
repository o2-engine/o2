# Root that will be mounted as /workspace inside the backend container.
# By default points two levels up — to the PetStory repo root, where Assets/
# and ContentDatabase/ live.
PROJECT_ROOT_HOST=../..

# Host ports
BACKEND_PORT=8765
FRONTEND_PORT=5173
